name: Weekly Afghan Security Digest

on:
  schedule:
    # 07:03 GMT every Monday (nudged 3 min off the hour — GitHub's docs note
    # exact-hour schedules are more prone to delay under load).
    - cron: "3 7 * * 1"
  workflow_dispatch: {}   # lets you trigger it manually from the Actions tab too

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install feedparser requests beautifulsoup4 anthropic python-dateutil

      - name: Generate digest
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API }}
        run: python generate_digest.py

      - name: Commit updated page
        run: |
          git config user.name "digest-bot"
          git config user.email "actions@github.com"
          git add index.html seen_links.json
          git commit -m "Digest update: $(date -u +'%Y-%m-%d')" || echo "No changes to commit"
          git push

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: .

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4
